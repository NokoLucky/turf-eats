rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for testing. Update: 2024-05-20T12:00:00Z
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}